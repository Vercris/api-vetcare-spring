spring:
  application:
    name: demo
  datasource:
    url: jdbc:mysql://localhost:3306/dbVeterinariaDAW
    username: root
    password: admin123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080